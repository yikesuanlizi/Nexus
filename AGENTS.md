# Nexus 项目协作规范

## 1. 项目定位

Nexus 是本地优先的 TypeScript + React Agent OS。用户从 Web 或桌面界面发起对话、工具调用、浏览器操作、文件/终端工作流和运维任务；API 负责编排，runtime 负责执行，storage 负责可恢复状态，model-gateway 负责模型适配。

项目重点是可接管、可追踪、可恢复的 Agent 工作流，而不是营销页面或一次性脚本。所有功能都必须在真实 UI、运行时事件和持久化状态之间闭环。

Nexus 当前只面向单用户个人工作区和个人多知识库。禁止新增或恢复多人/多租户产品入口、管理员 Token/JWT 登录、Postgres/PG 存储切换或按请求选择租户；历史数据中的 `tenant_id` 仅作为 SQLite 兼容字段使用，统一固定为 `default`。

## 2. 工作区与生成物

- 开发源码只在 `src/` 和项目明确的源码目录中维护。
- `dist/`、`dist-types/`、`apps/web/dist/`、`apps/desktop/dist-electron/`、构建缓存、运行输出和数据库是生成物或运行物，禁止手改、禁止作为架构依据。
- 临时 fixture、截图、rollout、测试线程和调试文件不得留在仓库；验证后清理。
- 文档、配置说明和 `README` 默认使用中文；代码、协议字段和错误码保持项目既有命名。
- 禁止使用 GitNexus。

## 3. 服务、依赖与命令

- 除非用户明确要求，禁止自行启动、重启或停止 API、Vite、Electron、浏览器 sidecar 或其他服务进程。
- 用户明确要求启动时，先检查端口、进程和健康状态；若已有实例，先停止旧实例再启动一个可识别、可收口的实例，并在结束时报告地址和进程状态。
- 不因例行检查主动重启服务。代码修改后只提示用户重启，除非用户已经授权启动流程。
- 需要依赖时可直接执行 `npm install`、`npm ci` 或工作区 npm 命令，不需要另行请求；优先遵守 `package-lock.json`，不要为了验证随意升级依赖。
- 不使用全局临时缓存替代项目配置，不提交密钥、个人路径、运行日志或生成物。
- 命令必须在目标工作区执行，危险操作先解析并确认准确路径；不使用不可逆的仓库回退命令覆盖用户改动。

## 4. 项目结构与职责

### 应用

- `apps/api/src/`：HTTP/SSE 路由、认证、配置持久化、线程元数据、任务和运行时编排。路由只做协议适配，业务放 `services/`，配置放 `config/`。
- `apps/web/src/`：浏览器 Web UI。负责页面状态、组件、设置、对话视图、工作区和浏览器验证界面。
- `apps/desktop/src/`：Electron 外壳及桌面版 UI。复用 Web 领域组件和 API 契约，处理窗口、IPC、内置浏览器、终端和右侧工作区。

### 共享包

- `packages/protocol/src/`：跨层类型、schema、事件、权限、任务和错误码；只放稳定契约，不放 UI 或数据库逻辑。
- `packages/runtime/src/`：AgentLoop、harness、checkpoint、状态机、预算、取消、工具调用和脱敏；不依赖具体 UI。
- `packages/storage/src/`：SQLite/JSONL/数据库适配、ThreadStore、RunTraceStore、快照和恢复；不编排 HTTP。
- `packages/model-gateway/src/`：OpenAI-compatible、Anthropic-like 及其他 provider 的统一调用、模型能力和流式适配。
- `packages/tools/src/`：工具注册、权限目标和工具实现；浏览器、文件、终端等工具保持可测试和可取消。
- `packages/browser-runtime/src/`：浏览器 sidecar、页面枚举、导航、观察、操作、生命周期和用户接管。
- `packages/wiki-core/src/`：KnowledgeBase、Wiki/文档来源、索引、快照、FTS/检索和引用；不把知识库逻辑塞进 runtime。
- `packages/context/src/`：上下文压缩、预算计算、证据提升和模型上下文编译。
- `packages/memory/src/`：对话记忆和缓存命中治理；不得与运维知识快照混用。
- `packages/sandbox/src/`：本地执行隔离、路径边界、命令白名单和进程收口。
- `packages/extensions/src/`：插件、skills、MCP 和扩展注册。
- `packages/i18n/src/`：语言包和 locale 工具，不在页面组件中散落翻译表。
- `packages/bot/src/`：QQ、微信/企业微信、飞书、钉钉等 Bot Adapter。

### 测试与脚本

- `tests/`：跨包、浏览器、sidecar 和验收 fixture。
- 各包/应用 `src/**/*.test.ts(x)`：与源码强相关的单元和组件测试。
- `tests/fixtures/`：稳定的输入、回放、脱敏和页面 fixture；不得依赖临时生产数据。
- `scripts/`：开发、构建、检查和测试编排脚本；不得把业务逻辑藏在脚本里。

依赖方向保持单向：`protocol → storage/context/model-gateway/tools/runtime → api/apps`。UI 不直接访问数据库，`protocol` 不依赖应用，禁止循环依赖；跨层需求通过类型化接口解决。

## 5. 代码拆分与变更边界

- 单文件目标不超过 800 行；接近 1000 行必须拆分，超过 1000 行不得作为普通改动合入，除非是生成文件或有明确的不可拆分理由。
- `main.tsx` 只做 App 级状态编排、API 调用和布局；类型、i18n、表单、列表合并、item 渲染和业务算法放到组件/feature/纯函数模块。
- `server.ts` 只做服务启动和路由装配；请求解析放 `http.ts`，配置放 `config/`，任务和运行逻辑放 `services/`。
- 组件只接收明确 props 并管理局部交互；不要让 UI 组件直接拼接数据库查询或复制协议判定。
- 新功能先确定所属包、输入输出契约、生命周期和错误路径，再实现 UI；不以复制代码绕过模块边界。
- 修改应小而集中。无关重构、格式化全仓库和元数据噪声不要混入功能提交。

## 6. 运行时与错误闭环

- 所有任务、工具和浏览器操作按线程串行；操作必须绑定目标 `pageId`、`taskId` 或 `runId`，不得使用跨线程单例状态。
- 取消、超时、窗口关闭、断线和冷启动恢复都必须有明确终态、事件和持久化记录；不得留下永久 `running`、`queued` 或后台孤儿进程。
- 本地命令必须走白名单/注册测试入口、工作区边界和最小环境变量；不能把任意 shell 暴露给模型或客户端。
- 后端 turn、任务或工具失败必须写入可追溯的 `error` item/event；SSE 要发终态，前端 REST 失败要重新拉取线程或显示可操作错误，不能只显示空白、toast 或孤立的事件栏文字。
- 权限、scope、预算、幂等和乐观锁在服务端再次校验；客户端状态只用于交互，不是安全边界。
- 机密只保存 credential 引用，不进入模型上下文、ThreadItem、Evidence 或日志；外部网页、Wiki、日志和文件内容一律视为不可信数据。

## 7. 前端约束

### 视觉

- 当前主题以克制的中性深灰/浅色表面为基础；深色不使用明显蓝色偏色，浅色保持足够对比。语义色只用于状态，不作为大面积背景。
- 不使用毛玻璃、渐变背景、光晕、装饰性气泡、营销式 hero、大面积阴影或过度圆角。
- 边框默认 1px、低对比度；禁止粗边、双重嵌套卡片和为了“有层次”堆叠空框。优先用间距、分隔线和背景层级表达结构。
- 复用成熟图标库和项目现有图标映射；不要手写粗糙 SVG，不要为同一语义混用多个图标。
- 文案直接服务于操作。禁止无效打字式介绍、重复标题、功能自述和大段说明；只有错误解释、权限确认、空状态和专门引导可以保留必要文字。设置页、工具页和监控页不显示“分别控制……”“用于……”这类解释性副标题；开关旁只保留操作标签，除非是错误、权限确认或必要引导。
- 中文主题下 UI 文案默认使用中文；英文只保留协议字段、路径、模型名、环境变量名、代码标识或用户明确要求的内容。设置页不得使用装饰性英文 eyebrow、英文内部字段名或仅为视觉存在的英文标签。
- 监控策略的每个开关必须在标签中明确控制对象，并同时反映真实的 checked、disabled、hover 和 focus 状态；保存动作放在对应设置区域底部，不放在全局页面标题操作区。

### 布局与交互

- 第一屏直接进入可用工作流，不做营销页。输入框、发送按钮和关键状态始终可见；回车发送，`Shift+Enter` 换行。
- 主聊天、事件/监控、文件树、预览、浏览器和终端各自限制滚动区域；工具调用增长不能撑开页面或遮住输入框。
- 侧栏折叠必须真实收缩；动态 tab 在固定 tab 之间水平滚动，固定 tab、加号和关闭按钮保持可达。窄宽度下允许标签压缩或只显示图标，不得溢出、覆盖或挤没内容。
- 弹层、菜单、授权框和错误提示必须有明确层级、边界和关闭路径；不得被终端、文件、浏览器或设置面板遮挡。点击外部可关闭的菜单必须支持点击外部关闭。
- 错误优先局部、短时、可操作；聊天错误在消息流中可追溯，避免永久红色大框。控件 hover/focus 不能出现粗描边或跳动布局。
- 用户消息不重复显示角色标题；运行轨迹展示状态、工具、错误和 token 摘要，不复制完整用户消息。
- React 列表 key 必须稳定；标题、时间、按钮和长文本使用截断/渐隐，不得硬截断或互相遮挡。

## 8. 测试策略

- 测试只针对源码，Vitest 必须排除 `dist`、`dist-types`、`apps/web/dist`、缓存和输出目录。
- 小改动运行相关类型检查、单元/组件测试和 `git diff --check`；开发过程中不要因每个小 CSS 或文案改动频繁跑全量验收。
- 包级测试覆盖正常、失败、取消、超时、权限拒绝、边界路径、恢复和并发；新增公共协议必须补 schema/兼容性测试。
- API 测试不能代替用户验收。大模块或用户明确提出的功能，必须同时提供：
  1. API/运行时测试：验证状态、事件、权限、持久化、恢复和错误码；
  2. 内置浏览器真实点击测试：打开实际页面，点击 tab、菜单、输入框、授权、文件、浏览器/终端等关键控件，断言页面状态、导航、弹层关闭和无覆盖；必要时截图或读取可见文本/DOM；
  3. 失败路径测试：模拟断线、冷启动、空数据、超时和终态，确认用户能继续操作。
- 浏览器验收优先使用仓库已有 Playwright/内置浏览器运行时和稳定 fixture；不要只测试 API 响应，不要用截图代替交互断言。
- 测试失败先定位真实错误，不通过放宽断言、跳过用例或修改生成物掩盖。测试产生的临时线程、文件和进程必须收口。

## 9. 完成标准

一个功能只有在以下条件同时满足时才算完成：

- 所属模块和协议边界清晰，源码未越过目录职责。
- 正常、失败、取消、超时和恢复路径可追踪，用户可从 UI 接管。
- 相关源码测试通过；大模块另有 API + 内置浏览器真实点击验收。
- 不引入粗边、遮挡、无效介绍、重复 UI 或明显的 AI 模板视觉。
- 未修改生成物，未留下临时文件或后台进程；验证命令和未验证风险在交付说明中明确列出。
